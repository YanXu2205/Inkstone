# Windows line endings

This file is stored with CRLF line endings, the way a Windows editor
would have saved it.

- a list item
- another item

> a quoted line

| A | B |
|---|---|
| 1 | 2 |

```ts
const crlf = true;
```

Final paragraph.
