# Tabs and a missing final newline

A nested list indented with tab characters:

-	first item
	-	nested with a tab
		-	deeper with two tabs
-	second item

An indented code block introduced by a tab:

	function tabbed() {
		return "indented with tabs";
	}

A paragraph, and this file deliberately ends without a trailing newline.