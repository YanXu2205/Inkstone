# Raw HTML

<div class="callout" data-tone="warning">
  <strong>Heads up:</strong> this block is raw HTML and must survive
  verbatim, attributes and indentation included.
</div>

Inline HTML in a paragraph: press <kbd>Ctrl</kbd>+<kbd>S</kbd> to save,
and see the <abbr title="Abstract Syntax Tree">AST</abbr> we deliberately
do not round-trip through.

<details>
<summary>Collapsed detail</summary>

A nested markdown paragraph with **bold** inside an HTML block.

</details>

<!-- an HTML comment with <tags> inside -->

An <span style="color: red">inline span</span> ends the file.
