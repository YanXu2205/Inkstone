# Compatibility matrix

| Feature | Status | Flag |
|:--------|:------:|-----:|
| Live preview | stable | `--preview` |
| Math via KaTeX | stable | `--math` |
| Mermaid diagrams | beta | `--mermaid` |
| Word export | beta | `--docx` |

A table with no leading or trailing pipes:

Column A | Column B
:------- | -------:
`code`   | plain
value    | 42

Prose after the tables.
